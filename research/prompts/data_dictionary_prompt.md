You are an expert software engineer specialized in the [Elixir](https://elixir-lang.org/) language. We are developing an Accounting and ERP system using the [Ash](https://hexdocs.pm/ash/readme.html) framework for persistance and the [Jido](https://hexdocs.pm/jido/readme.html) library for agentic work. The system will be architectured aroud multiple Elixir applications that will interact at runtime to create a whole system. Some of those applications may or may not be loaded during runtime. The attached document represent the data repository for one of those applications: Sales Order (a.k.a. SO). You can ignore the SQL procedures section of it. Your goal is to create a document containing an Ash domain and resources corresponding to the attached data dictionary. The domain should contain well defined Ash code interfaces corresponding to the ressources actions using the plurialized form of the resources and the corresponding Ash action. The top-level namespace name for the modules should be Accountex. * Regarding the ressources names in the domain they should not use the actual simple table name listed in the data directory but use a semantically correct long name. No ressource name should correspond exactly to the data dictionary table name ever. The table name for the postgresql configuration should not correspond to the data dictionary table name but instead reflect the resource long name using snake_case format. * You must not use the actual simplified name of the fields in the attached document but instead use a semantically correct long name based on its description when available. Field names must follow the snake_case format. No field name should correspond exactly to the original data dictionary field name. When the field description is not available you must use a semantically correct name based on its usage in the overall application domain. Commonly named fields like name, price, quantity, etc should explicitely name the resource it refers too. All field names should be unique accross the domain. The unique identifier field should be of type UUID7 and should be called id. You must respect the foreign key fields and use the long name of the table followed by _id. * You should include the appropriate relationships with foreign table using the appropriate section of the ressource when necessary. * An overral diagram of the domain should be included in the research document using a mermaid diagram.
